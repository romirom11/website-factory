packages:
  - "."

# esbuild (pulled in by tsx/drizzle-kit) needs its install script to select the
# correct native binary. pnpm 11 denies every dependency build not explicitly
# reviewed; keep that strict default and approve only this package family.
allowBuilds:
  esbuild: true
